# Public base URL of your backend API (used by Next rewrites and better-auth client)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Next.js runtime (optional overrides)
# NEXT_TELEMETRY_DISABLED=1

# If deploying behind HTTPS and cookies are Secure-only, ensure the backend issues cookies accordingly
# AUTH_COOKIE_DOMAIN=localhost
# AUTH_COOKIE_SECURE=false
# AUTH_COOKIE_SAMESITE=lax
